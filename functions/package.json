{
  "name": "functions",
  "scripts": {
    "build": "echo 'no build'",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "engines": { "node": "18" },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "@sendgrid/mail": "^8.1.3"
  }
}
